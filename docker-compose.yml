version: '3.8'

services:
  timetrace:
    build: .
    container_name: timetrace
    ports:
      - "8000:8000"
    volumes:
      - ./backend/src/data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=8000
      - DATA_DIR=/app/data
    restart: unless-stopped
